name: Build

on:
  push:
    branches: [ci-build]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build docker image
        working-directory: .
        run: docker compose build rpi
