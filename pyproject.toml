[project]
name = "eel-workspace"
version = "0.1.0"

[tool.mypy]
python_version = "3.10"
strict = true
ignore_missing_imports = true
mypy_path = "src/eel:typings"
packages = ["eel"]
exclude = ["^build/", "^install/", "^log/", "^venv/"]
